# User-defined boundaries

- The project name is exactly `math-reader-codex`. Use that name for repository, app, page and install metadata. Do not invent translated aliases, product names, release-status branding or page badges.
- Do not change page layout, CSS or icons without the user's explicit authorization. Do not add replacement branding in another part of the interface.
- Keep work in this independent repository. Do not modify the stable `none0enon/math-reader-boox` repository.
- Preserve the existing independent Android application ID, signing key and storage namespace when changing display names, so installed-app updates and user data remain compatible.
- An apology does not complete a requested correction. Verify the actual published page and signed APK before claiming that a correction is complete.
- Do not create or run tests for this repository unless the user explicitly reverses this instruction. Keep test files and dedicated test workflows out of the repository.
- Do not use sub-agents for this repository unless the user explicitly reverses this instruction.
- Cloud synchronization must never automatically publish an old or unconfirmed local-only file, and must never delete a local file merely because it is absent from cloud metadata. Preserve both sides and require an explicit manual upload confirmation for ambiguous local-only entries.
